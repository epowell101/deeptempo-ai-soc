# DeepTempo AI SOC - Environment Variables
# Copy this file to ~/.deeptempo/.env and fill in your actual values
# This is the recommended way to store secrets for server deployments

# =============================================================================
# Claude API Configuration
# =============================================================================
# Get your API key from: https://console.anthropic.com/
CLAUDE_API_KEY="sk-ant-api03-..."
# Alternative name (both work):
# ANTHROPIC_API_KEY="sk-ant-api03-..."

# =============================================================================
# AWS S3 Configuration (optional - for cloud storage)
# =============================================================================
AWS_ACCESS_KEY_ID="AKIA..."
AWS_SECRET_ACCESS_KEY="..."
# Note: Bucket name and region are configured via the web UI

# =============================================================================
# Timesketch Configuration (optional - for timeline analysis)
# =============================================================================
TIMESKETCH_PASSWORD="your_timesketch_password"
# Note: Server URL and username are configured via the web UI

# =============================================================================
# Splunk Configuration (optional - for SIEM integration)
# =============================================================================
SPLUNK_PASSWORD="your_splunk_password"
# Note: Server URL and username are configured via the web UI

# =============================================================================
# Cribl Stream Configuration (optional - for data pipeline management)
# =============================================================================
CRIBL_PASSWORD="your_cribl_password"
CRIBL_WORKER_GROUP="default"
# Note: Server URL and username are configured via the web UI

# =============================================================================
# GitHub Token (optional - for MCP GitHub server)
# =============================================================================
GITHUB_TOKEN="ghp_..."

# =============================================================================
# PostgreSQL Connection (optional - for MCP PostgreSQL server)
# =============================================================================
# Note: The MCP PostgreSQL server connection string is currently hardcoded in
# mcp-config.json. If you need to connect to a different database:
# 1. Edit mcp-config.json and update the "postgres" server args
# 2. Use format: postgresql://user:password@host:port/database
#
# Default for local development (matching docker-compose.yml):
# postgresql://deeptempo:deeptempo_secure_password_change_me@localhost:5432/deeptempo_soc
POSTGRESQL_CONNECTION_STRING="postgresql://deeptempo:deeptempo_secure_password_change_me@localhost:5432/deeptempo_soc"

# =============================================================================
# Secrets Backend Configuration
# =============================================================================
# Choose which backend to use for WRITING secrets:
#   - "dotenv" (default): Store in ~/.deeptempo/.env file
#   - "env": Store in process environment only (not persistent)
#   - "keyring": Use OS keyring (macOS Keychain, Windows Credential Manager)
# 
# For server deployments, use "dotenv" or set variables directly in your
# container/systemd environment. For desktop use, "keyring" is recommended.
SECRETS_BACKEND="dotenv"

# Enable keyring for READING secrets (default: false)
# Set to "true" only if you want to read secrets from OS keyring (macOS Keychain)
# Leaving this false prevents keychain permission prompts on macOS
ENABLE_KEYRING="false"

